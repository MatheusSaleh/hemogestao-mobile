import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
    userCard: {
        padding: 20,
        backgroundColor: "#f9f9f9",
        borderRadius: 5,
        marginBottom: 10,
        borderWidth: 1,
        borderColor: "#ccc",
    },
    userCardTitle: {
        fontSize: 16,
        fontWeight: "bold",
        marginBottom: 10,
    },
    userCardInfo: {
        marginBottom: 5,
        fontWeight: "semibold",
        fontSize: 16,
    }
})